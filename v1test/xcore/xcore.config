#redisHost = redis.t0.daoapp.io
#redisPort = 61426
#redisPass = wlniao
TaskCount = 5
TestUrl = http://api.juhe.cn
BetweenTime = 5
ActiveInterval = 15
CacheResult = false

#说明：TestUrl为测试的入口地址，TaskCount生成子线程的总数量，BetweenTime为生成子线程的间隔时间
#，ActiveInterval为线程活动间隔时间，单位为秒；可同时使用多台主机进行测试，配置Redis服务即可。